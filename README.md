# Exo api

```shell
git clone git@github.com:Magle-corp/exo_api.git
cd exo_api
npm install

npx parcel index.html # si parcel fonctionne à l'échelle de votre projet
parcel index.html # si parcel fonctionne à l'échelle de votre machine
```